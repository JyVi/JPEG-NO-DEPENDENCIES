#include "ImageEssential.hh"
#include <iostream>
#include <memory>

ImageEssential::ImageEssential(std::string ImageName)
{
    //WrapperImage wrapped = WrapperImage();
    std::cout << ImageName << std::endl;
    //this->width = wrapped.getSurface()->w;
    //this->height = wrapped.getSurface()->h;
    //this->Pixels = std::make_shared<void>(wrapped.getSurface()->pixels);
    //this->PixelFormat = std::make_shared<SDL_PixelFormat>(wrapped.getSurface()->format);
}
